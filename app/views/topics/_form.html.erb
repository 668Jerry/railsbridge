<%= form_for(@topic) do |f| %>
  <% if @topic.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@topic.errors.count, "error") %> prohibited this topic from being saved:</h2>

      <ul>
      <% @topic.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field row">
    <div class="col-md-2"></div>
    <div class="col-md-4"><%= f.label :title %></div>
    <div class="col-md-4"><%= f.text_field :title %></div>
    <div class="col-md-2"></div>
  </div>
  <div class="field row">
    <div class="col-md-2"></div>
    <div class="col-md-4"><%= f.label :description %></div>
    <div class="col-md-4"><%= f.text_area :description %></div>
    <div class="col-md-2"></div>
  </div>
  <div class="actions row">
    <div class="col-md-2"></div>
    <div class="col-md-4"></div>
    <div class="col-md-4"><%= f.submit %></div>
    <div class="col-md-2"></div>
  </div>
<% end %>